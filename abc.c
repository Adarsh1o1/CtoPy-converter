#include <stdio.h>
#include <conio.h>

int main()
{
    int a = -2;
    printf("sd");
    if (a > 0){
        printf("value of a is %d ", a);
    }else{
        printf("value of a is less than 0");
    }

    return 0;
}
